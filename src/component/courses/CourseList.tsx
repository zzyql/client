import React, { ReactElement } from 'react'

interface Props {
    
}

export default function CourseList({}: Props): ReactElement {
    return (
        <div>
            Course List
        </div>
    )
}
