import React, { ReactElement } from 'react'

interface Props {
    
}

export default function StudentList({}: Props): ReactElement {
    return (
        <div>
            Student list
        </div>
    )
}
